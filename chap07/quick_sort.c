/**
 * quick_sort.h
 * 快速排序
 */

#include "quick_sort.h"

int partition(int nums[], int len) {
    int small_top = -1;
    int key = nums[len-1];
    for (int i = 0; i < len; i++) {
        if ()
    }
}

void quick_sort(int nums[], int len) {

}
